export enum ChatType {
  CHAT = 0,
  GROUP_PRIVATE = 1,
  GROUP_FRIENDS = 2,
  GROUP_PUBLIC = 3,
}

export enum Cover {
  OUTDOOR = 2,
  BOTH = 1,
  INDOOR = 0,
}

export enum Gender {
  MALE = 0,
  FEMALE = 1,
  MIXED = 2,
}

export enum GroupType {
  PRIVATE = 1,
  FRIENDS = 2,
  PUBLIC = 3,
}

export enum MessageType {
  LOCATION = 2,
  TEXT = 0,
  PICTURE = 1,
}

export enum Sex {
  MALE = 0,
  FEMALE = 1,
}

export enum Status {
  PENDING = 0,
  ACCEPTED = 1,
  DECLINED = 2,
  BLOCKED = 3,
}
